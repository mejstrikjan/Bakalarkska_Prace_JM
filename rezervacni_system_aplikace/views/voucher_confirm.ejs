<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <title>Voucher potvrzen</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .confirm-wrapper {
      max-width: 600px;
      margin: 100px auto;
      text-align: center;
    }

    .confirm-box {
      background-color: rgba(15, 20, 25, 0.85);
      border: 2px solid #66fcf1;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 0 20px rgba(102, 252, 241, 0.3);
    }

    .confirm-box h2 {
      color: #66fcf1;
    }

    .qr-box img {
      border: 4px solid #66fcf1;
      border-radius: 8px;
      max-width: 100%;
    }

    .btn-primary.alt {
      background-color: #45a29e;
      border: none;
    }
  </style>
</head>
<body class="dark hero-bg">

  <%- include('partials/navbar', { user }) %>

  <div class="confirm-wrapper">
    <div class="confirm-box">
      <h2>Voucher úspěšně vytvořen 🎉</h2>
      <p class="lead mb-3">Kód voucheru: <strong><%= code %></strong></p>
      <p>Počet použití: <strong><%= uses %></strong></p>
      <p>Platnost do: <strong><%= expiration.toLocaleDateString('cs-CZ') %></strong></p>
      <p>Cena: <strong><%= price.toFixed(2) %> Kč</strong></p>

      <a href="/user" class="btn btn-primary alt mt-2">Zpět na profil</a>
    </div>
  </div>

</body>
</html>
