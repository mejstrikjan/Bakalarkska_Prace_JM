<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <title>Potvrzení rezervace</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background: url('/images/vr-bg.png') no-repeat center center fixed;
      background-size: cover;
      background-color: #0b0c10;
      color: white;
    }

    .container {
      max-width: 700px;
      margin: 60px auto;
      padding: 30px;
      background-color: rgba(15, 20, 25, 0.9);
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(102, 252, 241, 0.15);
    }

    h2, h5 {
      color: #66fcf1;
    }

    .form-container p strong {
      color: #66fcf1;
    }

    .btn-primary {
      background-color: #45a29e;
      border: none;
    }

    .btn-primary:hover {
      background-color: #3a8b89;
    }
  </style>
</head>
<body>

  <!-- NAVBAR -->
  <%- include('partials/navbar', { user }) %>


  <!-- OBSAH -->
  <div class="container text-center">
    <h2 class="mb-4">Rezervace byla úspěšně vytvořena</h2>

    <div class="form-container text-start">
      <p><strong>ID rezervace:</strong> <%= reservation.reservationID %></p>
      <p><strong>Datum konání:</strong> <%= new Date(reservation.reservationDate).toLocaleString('cs-CZ') %></p>
      <p><strong>Délka:</strong> <%= reservation.durationMinutes %> minut</p>
      <p><strong>Cena:</strong> <%= reservation.totalPrice.toFixed(2) %> Kč</p>
    </div>

    <div class="d-grid"><p><strong>Stav:</strong>
      <% if (reservation.status === 'paid') { %>
        <span class="status-badge status-paid">Zaplaceno</span>
      <% } else if (reservation.status === 'pending') { %>
        <span class="status-badge status-pending">Čeká na platbu</span>
      <% } else if (reservation.status === 'cancelled') { %>
        <span class="status-badge status-cancelled">Zrušeno</span>
      <% } else { %>
        <span class="status-badge bg-secondary text-white"><%= reservation.status %></span>
      <% } %>
    </p>
    
      <a href="/user" class="btn btn-primary mt-3">Zpět na profil</a>
    </div>
  </div>

</body>
</html>
